export const RESPONSE_MESSAGE = Object.freeze({
    CREATED: 'Server returns successful response',
    INTERNAL_SERVER_ERROR: 'Internal server error',
    NOT_FOUND: 'Bad request from the client',
} as const);
